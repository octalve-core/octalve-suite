{
  "name": "Approval",
  "type": "object",
  "properties": {
    "phase_id": {
      "type": "string",
      "description": "Reference to phase"
    },
    "project_id": {
      "type": "string",
      "description": "Reference to project"
    },
    "requested_at": {
      "type": "string",
      "format": "date-time"
    },
    "requested_by": {
      "type": "string"
    },
    "status": {
      "type": "string",
      "enum": ["pending", "approved", "changes_requested"],
      "default": "pending"
    },
    "responded_at": {
      "type": "string",
      "format": "date-time"
    },
    "responded_by": {
      "type": "string"
    },
    "feedback": {
      "type": "string"
    }
  },
  "required": ["phase_id", "project_id"]
}
